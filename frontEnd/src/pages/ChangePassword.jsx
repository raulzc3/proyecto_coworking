import { useParams } from "react-router-dom";
import useAuth from "../shared/hooks/useAuth";

export default function ChangePassword() {
  /*  let { changePasswordCode } = useParams();
  const { changePassword } = useAuth();*/

  return <h1>Change</h1>;
}
